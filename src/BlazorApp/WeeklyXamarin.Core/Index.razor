@page "/"
@inject AppState appState

<EditionsView />

@code {

    protected override void OnInitialized()
    {
        base.OnInitialized();
        appState.SetBackButtonState(false);
        appState.SetPageTitle(Constants.Navigation.Paths.Editions);
    }
}
